version: "3.8"
services:
  frontend:
    image: ${CR_URI}/frontend:v${{ github.event.pull_request.number }} # Use the correct image name
    ports:
      - "80:80" #  Remove if Nginx is handling the port forwarding
    restart: always